# Path to folder containing .z3, .z5, .z8 story files
STORIES=/zork/stories

# HTTP server port (default: 3000)
PORT=3000

# MongoDB connection URI (optional — sessions stored in-memory if unset)
# MONGODB_URI=mongodb://localhost:27017
