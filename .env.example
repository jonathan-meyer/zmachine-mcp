# Path to folder containing .z3, .z5, .z8 story files
STORIES=/zork/stories

# HTTP server port (default: 3000)
PORT=3000

# Redis connection URL (optional — sessions stored in-memory if unset)
# REDIS_URL=redis://localhost:6379
